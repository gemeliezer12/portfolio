const Hamburger = ({openState, openManager}) => {
    return (
        <div className={`dur25j4sw5 ${openState}`} onClick={openManager}>
            <div></div>
        </div>
    )
}

export default Hamburger
